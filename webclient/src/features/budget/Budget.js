export default class Budget {
    constructor(id, category, frequency, goal) {
        this.id = id;
        this.category = category;
        this.frequency = frequency;
        this.goal = goal;
    }
}